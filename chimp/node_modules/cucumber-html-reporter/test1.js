var sleep = require('system-sleep');
var reporter = require('cucumber-html-reporter');
var tStamp = Math.round(new Date().getTime()/10000); 
var prop = require("../properties-parser/index.js");

var options = {
	theme: 'bootstrap',
	//jsonFile: '../chimp/output.json',
	jsonFile: '..\\..\\..\\_f5\\output/report.json',
	//output: './cucumber_report.html',
	output: '..\\..\\..\\_f5\\output\\fico_rep.html',
	reportSuiteAsScenarios: true,
	launchReport: true,
	name:'FICO Report',
	storeScreenShots:true
};

//Store the file name in a property for email purpose
var editor1 = prop.createEditor();
//console.log(tStamp);
editor1.set("reportName",'fico_rep.html');
editor1.save("report.txt");
reporter.generate(options);
