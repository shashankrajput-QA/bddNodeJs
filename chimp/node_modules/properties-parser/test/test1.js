var fs = require("fs");
var prop = require("../index.js");

if (require("fs").existsSync("udhay.properties")) 
	var editor1 = prop.createEditor("udhay.properties");
else {
	var editor1 = prop.createEditor();
	editor1.save("udhay.properties");
};

editor1.set("basic", "prop1");
editor1.save;
console.log(editor1.get("basic"));
console.log(editor1.get("p"));